import React, { memo } from 'react'
import Leditor from '../../components/editor'

const EditorPage = memo(() => {
  return (
    <div>
    <Leditor></Leditor>    
    </div>
  )
})

export default EditorPage 